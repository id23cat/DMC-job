using Autofac;

namespace DMC.Weka.Operations.Implementations
{
    public class OperationsImplementationsModule : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}